name: Github Actions
on: 
  push:
  workflow_dispatch:
jobs:
  read_config_file:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Execute Build
        uses: ./.github/actions/builds/init